programa {
    funcao inicio() {
        inteiro N, numero, soma
      soma = 0
        escreva("digite um número:")
        leia(numero)
         para(i=1; i<=numero; i++) {
            soma = soma + i
         }
         escreva("soma=", soma)
    }
}
